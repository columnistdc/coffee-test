{
  "name": "@coffee-test/db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docker-compose up -d",
    "stop": "docker-compose down",
    "dev": "docker-compose up"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  }
} 